import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.bravetamarinswoop',
  appName: 'brave-tamarin-swoop',
  webDir: 'dist'
};

export default config;
